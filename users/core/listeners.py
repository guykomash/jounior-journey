from django.utils.dateparse import parse_datetime

def subscribe(data):
    print(f"subscriptions service consumed: {data}")

def unsubscribe(data):
    print(f"subscriptions service consumed: {data}")

